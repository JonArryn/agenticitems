info:
  name: get item by id
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}:{{port}}/{{version}}/item/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
